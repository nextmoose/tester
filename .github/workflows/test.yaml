name: test
on:
  push: 
jobs:
  env:
    runs-on: ubuntu-latest
    steps:
      - run : env
  pre-check:
    runs-on: ubuntu-latest
    steps:
      - run: true
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: cachix/install-nix-action@v17
        with:
          extra_nix_configs: access-tokens = github.com=${{ secrets.token }}
      - run: cd .github/workflows/check && echo ${GITHUB_WORKFLOW} && nix develop --command check
