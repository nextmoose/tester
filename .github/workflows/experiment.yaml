name: "experiment"
on:
  push:
jobs:
  experiment:
    runs-on: ubuntu-latest
    steps:
      - uses: nextmoose/tester@f30d783097b2f35873600594e67b81a06236744f
        with:
          implementation-url: github:nextmoose/tester
          implementation-postulate: false
          test-url: github:nextmoose/tester-test
          test-postulate: false
          test-rev: 
          defect: 
          postulate: false
          token: ${{ secrets.TOKEN }}
