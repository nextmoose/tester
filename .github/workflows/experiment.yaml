name: "experiment"
on:
  push:
jobs:
  experiment:
    runs-on: ubuntu-latest
    steps:
      - uses: nextmoose/tester@6f95b9a890a6afd785760ad1e65aac2f903e3910
        with:
          implementation-url: github:nextmoose/tester
          implementation-postulate: false
          test-url: github:nextmoose/tester-test
          test-postulate: false
          test-rev: 
          defect: 
          postulate: false
          token: ${{ secrets.TOKEN }}
